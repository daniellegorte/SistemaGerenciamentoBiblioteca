-- CRIANDO BANCO DE DADOS
CREATE DATABASE biblioteca;
USE biblioteca;

-- CRIANDO TABELAS
CREATE TABLE usuario (
    idUsuario INT AUTO_INCREMENT PRIMARY KEY, 
    nome VARCHAR(100) NOT NULL, 
    email VARCHAR(100) NOT NULL,
    telefone VARCHAR(11) NOT NULL,
    dataCadastro DATE NOT NULL,
    tipo VARCHAR(100) NOT NULL
);


CREATE TABLE livro (
    idLivro INT AUTO_INCREMENT PRIMARY KEY, 
    titulo VARCHAR(100) NOT NULL,
    autor VARCHAR(100) NOT NULL,
    editora VARCHAR(100),
    anoPublicacao YEAR,
    qntTotal INT NOT NULL,
    qntDisponivel INT NOT NULL,
    disponivel TINYINT(1) NOT NULL DEFAULT 1 -- DISPONIVEL = 1, INDISPONIVEL = 0
);

CREATE TABLE emprestimo (
    idEmprestimo INT AUTO_INCREMENT PRIMARY KEY, 
    dataEmprestimo DATE NOT NULL,
    dataDevPrevista DATE NOT NULL,
    dataDevReal DATE,
    idLivro INT NOT NULL,
    idUsuario INT NOT NULL,
    
    -- Relaciona a Foreign Key com a Primary Key de outra tabela
    CONSTRAINT fkIdLivro FOREIGN KEY (idLivro) REFERENCES livro(idLivro),
    CONSTRAINT fkIdUsuario FOREIGN KEY (idUsuario) REFERENCES usuario(idUsuario) 
);

-- POPULANDO TABELAS COM VALORES INICIAIS
INSERT INTO usuario (nome, email, telefone, dataCadastro, tipo) VALUES
('Arthur Araujo', 'araujo@email.com', '11987654321', '2026-01-10', 'Aluno'),
('Danielle Soares', 'soares@email.com', '11976543210', '2026-01-12', 'Aluno'),
('Roberta Lansana', 'lansana@email.com', '11965432109', '2026-01-15', 'Aluno'),
('Carlos Eduardo Mattos', 'mattos@email.com', '11954321098', '2026-02-01', 'Professor');

INSERT INTO livro (titulo, autor, editora, anoPublicacao, qntTotal, qntDisponivel, disponivel) VALUES
('Código Limpo: Habilidades Práticas do Agile Software', 'Robert C. Martin', 'Alta Books', 2009, 5, 4, 1),
('O Codificador Limpo', 'Robert C. Martin', 'Alta Books', 2012, 3, 3, 1),
('Design Patterns: Soluções Reutilizáveis de Software Orientado a Objetos', 'Erich Gamma', 'Bookman', 2000, 2, 1, 1),
('Entendendo Algoritmos', 'Aditya Y. Bhargava', 'Novatec', 2017, 4, 4, 1),
('Java: Como Programar', 'Paul Deitel', 'Pearson', 2016, 3, 0, 0);


INSERT INTO emprestimo (dataEmprestimo, dataDevPrevista, dataDevReal, idLivro, idUsuario) VALUES
('2026-03-01', '2026-03-15', '2026-03-10', 1, 1),
('2026-03-05', '2026-03-19', '2026-03-18', 2, 2),
('2026-03-10', '2026-03-24', NULL, 1, 3),
('2026-03-12', '2026-03-26', NULL, 3, 4);


